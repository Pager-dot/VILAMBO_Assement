# Used automatically by `vite build`. Points the built frontend at the deployed
# backend. Not secret (public API URL). Override in your host's dashboard if the
# backend URL changes.
VITE_API_BASE=https://vilambo-assement.onrender.com
